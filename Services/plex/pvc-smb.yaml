kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc-plex-smb
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 3000Gi
  volumeName: pv-plex-smb
  storageClassName: smb
